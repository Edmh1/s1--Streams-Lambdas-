package practica;

public class Dia5 {
    public static void main(String[] args) {
        // 🔹 1. Filtra los nombres que tengan exactamente 6 letras y contengan la letra "a"
        // 💡 Resultado esperado: lista de nombres con 6 letras que contienen "a"

        // 🔹 2. Convierte todos los nombres a minúsculas y elimínalos si aparecen más de una vez
        // 💡 Resultado esperado: lista de nombres en minúscula sin duplicados

        // 🔹 3. Agrupa los nombres por la última letra
        // 💡 Resultado esperado: Map<Character, List<String>> con clave como última letra

        // 🔹 4. Crea un Map<String, Integer> con el nombre y su longitud, sin duplicados
        // 💡 Resultado esperado: {"Ana"=3, "Pedro"=5, ...}

        // 🔹 5. ¿Cuántos nombres únicos comienzan con vocal?
        // 💡 Resultado esperado: número entero

        // 🔹 6. Obtén una lista con los nombres únicos, en mayúsculas, ordenados alfabéticamente, que tengan más de 4 letras
        // 💡 Resultado esperado: lista de nombres

        // 🔹 7. Devuelve un mapa donde la clave es true si el nombre contiene la letra "i", y el valor es la lista correspondiente
        // 💡 Resultado esperado: Map<Boolean, List<String>>

        // 🔹 8. Calcula la suma total de las longitudes de los nombres que terminan en vocal
        // 💡 Resultado esperado: entero (suma)

        // 🔹 9. Agrupa los nombres por longitud y muestra solo los grupos cuya longitud sea impar
        // 💡 Resultado esperado: Map<Integer, List<String>> solo con claves impares

        // 🔹 10. Genera un resumen estadístico de la longitud de los nombres únicos
        // 💡 Resultado esperado: IntSummaryStatistics (promedio, min, max, suma, count)

        // 🔹 11. A partir de una lista de objetos Persona, agrúpalas por ciudad
        // 💡 Resultado esperado: Map<String, List<Persona>> agrupadas por ciudad

        // 🔹 12. Filtra los números impares mayores a 10 y calcula su producto
        // 💡 Resultado esperado: entero (multiplicación acumulada)

        // 🔹 13. Agrupa los nombres por su segunda letra (si existe)
        // 💡 Resultado esperado: Map<Character, List<String>>

        // 🔹 14. Convierte una lista de strings a formato tipo JSON: ["Ana","Pedro"] → {"Ana", "Pedro"}
        // 💡 Resultado esperado: String con formato {"Ana", "Pedro"}

        // 🔹 15. Dado un Map<String, Integer> con nombres y edades, muestra solo los nombres con edad mayor a 30
        // 💡 Resultado esperado: lista de Strings con nombres

    }
}
